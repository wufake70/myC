#include <stdio.h>

int main()
{
    // 定义指针变量,4字节
    // *取值运算法，&取地址运算法
    int *p1,*p2;
    int a = 88;
    // 取地址值付给指针
    p1 = &a;
    printf("p1=%p\n",p1);
    // 通过指针取变量值(解引用)
    printf("p1指向的变量值%d\n",*p1);

    // 避免访问未初始化的指针(野指针)
    // *p2 = 999;
    

}